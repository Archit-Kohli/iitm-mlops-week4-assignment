# Core ML Libraries (Pinned to solve model loading error)
scikit-learn==1.0.2
pandas==1.3.5
joblib==1.1.0

# DVC and GCS (Pinned to versions compatible with Python 3.8)
dvc==2.58.2
dvc-gcs==2.21.0

# Testing and CI/CD
pytest
cml